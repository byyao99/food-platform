# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

A food-ordering platform: a Go + Gin REST API (repo root) serving **Menu** and **Order** resources, plus a Vue 3 + TypeScript SPA (`frontend/`) that consumes it. Persistence is SQLite via GORM, using the pure-Go driver `github.com/glebarez/sqlite` (no cgo). The DB file path defaults to `food-platform.db` and is overridable with the `DB_PATH` env var.

## Commands

Backend (run from repo root):

```bash
go run .                    # start API on :8080 (PORT env var overrides)
go build ./...              # build
go vet ./...                # vet
```

Frontend (run from `frontend/`):

```bash
npm install
npm run dev                 # Vite dev server on :5173, proxies /api -> :8080
npm run build               # vue-tsc --noEmit type-check, then vite build
npm run type-check          # type-check only
```

To work on the full stack, run the backend and `npm run dev` simultaneously; the Vite proxy (`frontend/vite.config.ts`) forwards `/api/*` to the backend, so the SPA uses same-origin relative URLs. There is no test suite yet.

## Architecture

**Backend layering** (`internal/`): requests flow `router → handlers → store → models`. The `store.Store` wraps a `*gorm.DB`; it is created once in `main.go` via `store.Open(dsn)` (which opens the DB and runs `AutoMigrate`) and injected into handlers. String UUID primary keys are generated by the handlers (`uuid.NewString()` when building the model on create), not by the store — the store only persists what it's given, and GORM manages `CreatedAt`/`UpdatedAt`. `OrderItem.ID` is the exception: it's a GORM autoincrement assigned by the DB. Handlers never touch GORM directly. When adding a new create path, remember to set the UUID in the handler. The store translates GORM's `gorm.ErrRecordNotFound` into the sentinel `store.ErrNotFound`, which `handlers.respondStoreError` (in `menu.go`, shared by both handlers) maps to HTTP 404; any other store error becomes a 500. Note that `ListMenu`, `CreateMenuItem`, `ListOrders`, and `CreateOrder` return errors (unlike the original in-memory version), so their handlers check and forward via `respondStoreError`.

**Order/OrderItem is a GORM has-many.** `Order.Items` maps to a separate `order_items` table via `foreignKey:OrderID;constraint:OnDelete:CASCADE`. `OrderItem.ID` (autoincrement) and `OrderID` are persistence-only and hidden from the API with `json:"-"`, so the request/response JSON is unchanged from the in-memory version. Reads use `Preload("Items")`; `CreateOrder` saves the order and its items in one call (GORM auto-saves the association); `DeleteOrder` removes items then the order inside a transaction (cascade is enforced in code rather than relying on the SQLite `PRAGMA foreign_keys`).

**Money is integer cents everywhere.** All monetary fields (`price`, `unit_price`, `subtotal`, `total_amount`) are `int64` cents, never floats — this keeps arithmetic exact. The API speaks cents; the frontend converts dollars ↔ cents only at the UI edge via `frontend/src/money.ts` (`toCents`/`fromCents`/`formatCents`). When adding a money field, keep it `int64`.

**Order pricing is server-authoritative.** When creating an order, the client sends only `menu_item_id` + `quantity`. `OrderHandler.buildOrderItems` looks up each menu item, rejects unknown or unavailable items (400), and computes `unit_price`, `subtotal`, and `total_amount` (cents) from the menu — client-supplied prices are never trusted. Each `OrderItem` stores a **snapshot** of the name and price at order time, so later menu edits do not alter historical orders.

**Order updates are status/note only, gated by a state machine.** `PUT /orders/:id` accepts just `status` and `note`; line items cannot be changed after creation. The handler validates the change with `OrderStatus.CanTransitionTo` (table in `internal/models/models.go`): `pending → preparing → ready → completed`, `cancelled` reachable from any non-terminal state, and `completed`/`cancelled` are terminal. Illegal transitions return 400. The frontend mirrors this table in `types.ts` (`statusOptions`) so the UI only offers legal next statuses — keep the two in sync.

**List endpoints are paginated and sortable.** `GET /menu` and `GET /orders` take `?limit` (default 20, max 100), `?offset`, `?sort`, and `?order`, parsed by `handlers.parseListOptions` into a `store.ListOptions`. Their response shape is `{"data": [...], "pagination": {limit, offset, total}}` — different from the single-resource `{"data": ...}` shape. The frontend's `requestPage` reads both; single-object calls use `request`. **Sorting is SQL-injection-safe by construction:** `store.orderClause` only emits a column name if it's in a per-resource allowlist (`menuSortColumns`/`orderSortColumns`), else falls back to the default; direction is restricted to `asc`/`desc`. Never interpolate `ListOptions.Sort` into a query directly — add new sortable columns to the allowlist instead.

**API response shape:** single resources are `{"data": ...}`, errors `{"error": "..."}`, lists add a sibling `pagination` block. `respondStoreError` (in `handlers/common.go`) maps `ErrNotFound`→404 and logs+500s everything else.

**Frontend** is a two-view SPA (vue-router: `/menu`, `/orders`) plus a shared `components/PaginationBar.vue`. `frontend/src/types.ts` mirrors the Go models and is the contract between the two halves — keep it in sync when changing JSON shapes. `api/client.ts` is the only place that talks HTTP; views call `menuApi` / `orderApi` and hold their own local state. In `OrdersView`, the menu dropdown fetches a large page (limit 100) to list all available items, while the orders list itself is paginated.

## Conventions

- Adding a backend resource means touching all layers: model in `internal/models` (with GORM tags, and registered in the `AutoMigrate` call in `store.Open`), CRUD methods on `store.Store`, a handler struct with a `New*Handler(s *store.Store)` constructor, and route registration in `internal/router/router.go`.
- Migrations are automatic (`AutoMigrate` on startup), which adds columns/tables but does not drop or alter existing ones; destructive schema changes must be handled manually.
- CORS is permissive (`internal/router/router.go`) for local dev; tighten before any real deployment.
- `main.go` runs the server under `http.Server` with signal-driven graceful shutdown (SIGINT/SIGTERM → `Shutdown` with a 10s drain → `store.Close`). `GET /health` calls `store.Ping` so it reflects DB connectivity.
- Code comments and all user-facing strings are in English.
